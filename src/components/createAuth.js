import React from 'react';
import Firebase from '../utils/firebase';

class CreateAuth extends React.Component {


    render() {
        return(
            <div>

            </div>
        );
    }
}